# CONFIGURATION BANNER
spring:
  banner:
    location=classpath : /banner.txt

# GENERAL CONFIGURATION SERVER (ENVIRONMENTS DEV, HML AND PRD)
server:
  servlet:
    context-path : /f1-challenge
  port : 5000
